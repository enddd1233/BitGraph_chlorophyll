# Checkpoints

This directory is reserved for optional pretrained model checkpoints.

Checkpoint files such as `*.pth`, `*.pt`, and `*.ckpt` are ignored by Git by default because they can be large. If pretrained weights are released, place small files here or provide a download link through GitHub Releases, Zenodo, Figshare, or another long-term archive.
