# Results

This directory is reserved for lightweight result summaries, such as aggregated MAE/RMSE/MAPE CSV files reproduced from `main.py`, `test_forecasting.py`, `run_all_experiments.py`, or `batch_evaluate_masked.py`.

Large runtime outputs, logs, prediction arrays, generated figures, and full experiment folders are intentionally excluded from version control.
